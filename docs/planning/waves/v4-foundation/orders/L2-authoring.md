# L2 — data authoring and deterministic region compilation

Own `hex_schematic::v4` and `assets/config/v4`. All numbered manifest decisions bind
this lane. Consume [public contracts](../maps/contracts.md); report contradictions.
Keep source specification separate from compiled output. Build exact generic interval,
landform, basin/channel, graded route, bridge, cave, local override and feature
operations. Preserve full radius-187 dimensions and return explicit diagnostics.
Compiler dependencies must be bounded/declared; one shared boundary resolver controls
both neighbors. Runtime source changes must never require include_str or Rust edits.

Prove a materially different rich full-size region, two adjacent regions and seven
regions; use independent expected topology and negative fixtures rather than only
hash self-consistency. Coordinate package wiring with the coordinator. Commit your
source branch; report APIs, exact checks and remaining limitations.
