# Finite expedition carving and lowland encounters

The approved combat/world injection keeps the initial V4 package strict and makes the live expedition destructible. Generic persistent V4 transactions and other arena maps retain their previous admission rules.

`hex_world_runtime::FiniteWorldSession` retains immutable resident chunk products and stores sparse exact terrain assignments plus original-object removals. One `WorldEditTransaction` covers terrain and all overlapping object contributors. Expected chunk revisions and request fingerprints enforce atomicity and duplicate prevention. Existing water cells and finite column/height bounds remain immutable; live support, anchor and route validity may change. Dropping the session restores the original package.

Map damage resolves one material/HP value per exact physical cell. Existing solid durability is retained; formerly toughnessless bedrock gets 8 HP. Terrain and object removals are committed before the shared `ArenaTerrainView` publishes dirty columns, surviving material intervals and static collision intervals. Local unrotated `ObjectCarveMask` coordinates drive renderer-owned section rebakes, including cut-face exposure. Every expedition feature starts with an empty mask; unsupported crowns and props remain in place. Initial source blueprints and their colors are unchanged.

The lowland package retains the forest's 807 trees, 107 props, six fountains and 67.01% canopy coverage. It adds independent Golem sites at (15,72), (5,115), (35,137), Wisp packs at (35,60), (20,95), (0,145), and seven winding path segments. All six sites are graded to level 40. Golem spaces reserve 16 clear levels; Wisp spaces reserve 96 before vegetation placement. Gameplay owns species, counts, movement and rewards.

Compiled package fingerprint: `8f56a9974aaac54b`. Terrain-only fingerprint: `3e1fd17a3c1145f3`. Source artwork remains bound to `89940ef4cb040a30506be21206c31dbdabd3f087`. Public survey verification covers 105,469 terrain columns, 914 exact object identities, 4,908 ground contacts, 9,497 site supports, 49 routes and 25 encounters. The isolated package is `work/combat-world/assets/config/v4/forest-massif/expedition/compiled`; keep the user-owned running game's older package untouched.

Transparent expedition water emits the closed boundary of the actual voxel union, including sides and undersides. Internal water faces are culled against neighboring columns across render chunks. Every boundary batch preserves fountain identity, so charged and spent materials apply to every face. World-owned liquid state and physics do not change when its vessel is carved. Root integration owns the camera underwater treatment.

Exposed forest terrain sides are subdivided only where visible, with faint voxel seams and a stronger seam every four levels. Restrained color variation retains the original material palette; a small base-colored emission keeps shaded surfaces readable. Four-percent edge normals add modest corner shading without changing the physical boundaries. Other world meshes retain their prior geometry. Root integration owns sun and moving clouds.

Evidence completed: three finite-runtime atomicity/idempotency/reset/water tests; strict runtime library Clippy; 47 Python authoring/placement/package/structure verification tests; strict terrain and complete-package compiler/survey verification. The combined APP feature closure compiled, the two new closed-water mesh tests passed, and five existing arena real-world regressions passed. The new exact-expedition carving/reset test remains explicitly ignored until the combined package is selected. Object publication now belongs to the shared `PublishObjects` set so renderer reconciliation can consume masks in the same frame. That final scheduling tag, indexed object lookup and four-percent edge-normal selection postdate the focused APP run and require the integration owner's combined-head checks. Exact 128-actor runtime/performance checks and fresh windowless pixels remain pending. The user handles short native feel/UI checks.
