# Native restart feedback, September 12

User reported that the game closed after death, when pressing Restart. The native Cargo launch used candidate e821670e6e52d27dd5f375419afa8ac09c19089c, package a538263d612e891f. The helper exited with status 0 and a Winit destroyed-window warning, with no panic or asset failure. Native click count/timing and any user window resizing were not recorded.

At the configured 1600×900 / scale 1 layout, Restart occupies x792–1052, y640–682. The revealed ready-menu Quit occupies x806–1078, y645–687. A second click at Restart's center therefore presses Quit. Independent source/pixel review identified this overlap; a new test using actual Bevy UI layout/focus and MouseButtonInput reproduces AppExit::Success on the second press. A held first press alone does not migrate to Quit. The user's exact click timing remains unproven; the hazardous transition is reproduced.

Commit 304908c moves the primary Start action below the secondary ready-menu buttons. This keeps Quit away from the old Restart rectangle and makes a repeated click select Start. The actual expedition test passes at 1600×900, 1280×720 and 1920×1080, including ordinary defeat, terrain/session reset, pointer press/release, non-overlapping Quit bounds and a second real hit-tested press. Six prior terminal-menu regressions and the existing computed-layout test pass. Logs: `outputs/expedition-validation/restart-pointer-reproduction.log`, `restart-pointer-fixed.log`, `restart-menu-regressions.log` and `restart-menu-layout.log` in the outer workspace.

The user requested an immediate relaunch during investigation. Session 46006 is their live, earlier e821670 build, launched through Cargo; the log confirmed a native window and ready state. Do not terminate that session to install these edits without considering their active run. Runtime changes require another launch. Timer remains paused. Fresh menu pixels and the final combined build are recorded in the next checkpoint; the old 26-view map pack remains map/art evidence, not evidence of the revised menu.
