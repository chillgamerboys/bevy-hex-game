# L1 — Macro spanning-feature contracts

Implement only the serialized/resolved contracts and fingerprint closure listed in the
manifest. Preserve D1, D2, D5, and D6 verbatim. Add defaulted explicit walker connections,
anchor aliases, and one initial tunnel spanning-feature schema; validate stable names,
ordered instance routes, boundary/destination references, levels, width, clearance, and roof.
Allow an empty legacy critical route only when exactly one spanning feature supplies it.
Resolve exact subsurface seam lane pairs separately from surface walker ports. Conditionally
encode nonempty extensions so every existing fingerprint remains identical. Add focused
boundary, malformed-RON, legacy fingerprint, layout coverage/connectivity, and deterministic
resolution tests. Do not edit world composition or scenario content.
