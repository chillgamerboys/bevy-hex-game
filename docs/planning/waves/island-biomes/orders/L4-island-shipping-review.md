# L4 — Selectable content, lifecycle, walks, documentation, and review pack

Read the complete parent manifest first. You own only the L4 paths and regions listed there.

Implement locked decisions I2, I3, and I6 verbatim without adding generation policy. Add
the Sandy Islets, Wooded Island, and Ocean Archipelagoes worlds/scenarios/Sandbox cards,
save dependency inputs, lifecycle coverage, preview assets, route registry, documentation,
and exact camera-walk contracts. Derive walk waypoints from production traversal; never
teleport across water or infer island logic from pixels. Every movement step is
Click → Settle(5) → AwaitPartyIdle → exact arrival.

The coordinator owns actual game launches and captures. Run focused asset/app/lifecycle,
formatting, diff, and static route tests, then hand off the exact scenario names, anchors,
and capture matrix.
