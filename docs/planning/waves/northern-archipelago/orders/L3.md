# L3: Exploration controller

Authority: gameplay. Branch: `feat/northern-flight`. Follow manifest locked decisions. Own only the declared paths/regions. Root owns shared integration edits. Read [survey](../maps/survey.md). If source differs materially, report it before changing the contract. Focused checks only while moving; serialize Cargo through root. Commit identifiable local work and report executed counts plus limitations. Never operate native apps or overwrite another package.
