# L1 — strict schematic contracts and traced template

Implement S1–S6's strict versioned model, canonical radius-eight coordinate order,
layered cell facts, provenance, feature/network vocabulary, and designer template schema.
Add the complete Grand V3 revision-2 reference template transcribed cell for cell from the
supplied sketch. The fixed, unrotated flat-top orientation uses `x = 1.5q` and
`y = sqrt(3)(r + q/2)`. Lock twelve north-eastern peaks as two six-cell chains, the
mountain lake and lake island, the frozen three-cell core and its single mountain-shore
contact, the waterfall opening, and the straight `q = 1` tunnel. Represent the river as an
overlay over land until it reaches the sea. Revision 2 explicitly replaces the prior
approximate trace. Reject unknown fields and malformed or duplicate stable identifiers.
Do not implement solver, renderer, CLI, or runtime V3 integration.
