# L2 — generator, validator, metrics, and fingerprints

Implement S1–S6's canonical grid, independent named seed streams, 32-candidate constructive
planner, reference fallback, hard validator, deterministic semantic fingerprint, and public
metrics. Establish coast, hydrology, landforms, islands, and woodland in that order. Never
emit an invalid plan and keep every rule renderer-independent.
