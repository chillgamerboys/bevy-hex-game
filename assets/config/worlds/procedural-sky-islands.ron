// Structural landform probe. Optional islands are deliberately unreachable by the
// ordinary walker; all scenario anchors remain on the linked critical chain.
(
    grid_radius: 12,
    level_height: 0.4,
    terrain: Procedural((
        generator_version: 1,
        landform: SkyIslands((
            surface_level: 15,
            island_radius: 3,
        )),
        environment: TemperateGrassland,
        tactical: LinkedIslands((
            bridge_width: 2,
        )),
    )),
)
