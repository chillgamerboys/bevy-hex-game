// How the camera moves.
(
    // Reapplied whenever gameplay starts so the complete radius-12 map is visible.
    gameplay_eye: (0.0, 48.0, 42.0),
    gameplay_focus: (0.0, 6.0, 0.0),

    // Close third-person view around the selected character.
    character_focus_height: 0.4,
    character_radius: 7.0,
    // Terrain collision affects only Character mode. The camera snaps inward before
    // the closest public tile span without changing the player's look direction or
    // desired zoom. Outward recovery begins only after clearance remains stable, so
    // walking past adjacent obstructions cannot make the view repeatedly breathe.
    // The sphere probe plus collision margin conservatively protect the near plane
    // around prism corners.
    character_probe_radius: 0.1,
    character_collision_margin: 0.35,
    character_restoration_speed: 8.0,
    character_collision_release_delay: 0.2,
    // At near-first-person distances the selected mesh would cover the view. Hide
    // it until the collision boom recovers beyond this radius (plus hysteresis).
    character_self_hide_radius: 1.0,
    // Signed quarter-turn fraction: -1.0 is straight up, 0.0 is the horizon,
    // and 1.0 is straight down.
    character_pitch: 0.3,

    // WASD pan speed. Scaled by how far out you are zoomed, so panning feels the
    // same at every distance.
    pan_speed: 0.4,
    // Added to the zoom distance before scaling pan speed, so panning still works
    // when fully zoomed in.
    pan_speed_offset: 10.0,

    // Map-view tilt limits, 0.0 (level with the horizon) to 1.0 (straight down).
    min_pitch: 0.25,
    max_pitch: 0.95,

    // Zoom limits, in world units.
    min_zoom: 5.0,
    max_zoom: 70.0,

    // Fraction of the current distance covered per scroll notch.
    zoom_sensitivity: 0.2,
)
