//! RON asset definitions, loaders, and load tracking.
//!
//! Placeholder. This crate exists now so the workspace shape is visible and so
//! the content pipeline lands as a purely additive change: the RON settings
//! types replace `hex_core::config` and the loading screen waits on the load
//! tracking defined here.

use bevy::prelude::*;

/// Registers asset types and loaders.
pub fn plugin(_app: &mut App) {}
