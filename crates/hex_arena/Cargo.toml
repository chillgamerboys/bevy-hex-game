[package]
name = "hex_arena"
version.workspace = true
edition.workspace = true
publish = false
description = "Headless continuous movement and spell authority for the opt-in duel arena."

[features]
default = []
# Frozen previous bot and explicit evaluation selection, never in ordinary play.
test-support = []

[dependencies]
bevy_app.workspace = true
bevy_ecs.workspace = true
bevy_math.workspace = true
hex_core.workspace = true
serde.workspace = true
ron.workspace = true

[lints]
workspace = true
