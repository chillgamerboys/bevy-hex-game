[package]
name = "hex_ui"
version.workspace = true
edition.workspace = true
publish = false
description = "Responsive runtime presentation and typed UI contracts for the hex game."

[dependencies]
bevy = { workspace = true }
hex_assets = { workspace = true }
hex_core = { workspace = true }
hex_gameplay_model = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
ron = { workspace = true }

[features]
default = []
dev-tools = []
test-support = []
visual-review = ["bevy/bevy_ui_debug"]

[lints]
workspace = true
