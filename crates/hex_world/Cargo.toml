[package]
name = "hex_world"
version.workspace = true
edition.workspace = true
description = "3D presentation of the world: hex grid spawning, terrain meshes, sky, camera."

# Must NOT depend on hex_gameplay. The two talk through components and states
# defined in hex_core; cargo is what enforces that.
[dependencies]
bevy = { workspace = true }
hex_core = { workspace = true }
