[package]
name = "hex_world"
version.workspace = true
edition.workspace = true
publish = false
description = "3D world presentation: sky and camera."

# Must NOT depend on hex_gameplay. The two talk through components and states
# defined in hex_core; cargo is what enforces that.
[dependencies]
bevy = { workspace = true }
hex_assets = { version = "0.1.0", path = "../hex_assets" }
hex_core = { workspace = true }

[lints]
workspace = true
