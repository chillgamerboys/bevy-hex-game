[package]
name = "hex_world"
version.workspace = true
edition.workspace = true
publish = false
description = "3D world presentation: sky and camera."

# Must NOT depend on hex_units. The two talk through components and states
# defined in hex_core; cargo is what enforces that.
[dependencies]
bevy = { workspace = true }
hex_assets = { workspace = true }
hex_core = { workspace = true }

[dev-dependencies]
hex_test_app = { workspace = true }

[features]
default = []
test-support = []

[lints]
workspace = true
