// Real descent from the opaque-roof cave entrance to the root chamber.
[
    AwaitScreen("Title"),
    StartScenario(name: "Caves", seed: Some(736283041)),
    AwaitScreen("Gameplay"),
    AwaitTerrain,
    Key("H"),
    Key("C"),
    Settle(45),
    Capture("01-caves-character-entrance"),
    ClickAnchor(name: "conflict_center", expected: (q: 0, r: 0, level: 6)),
    AwaitPartyIdle(max_frames: 2400),
    Settle(60),
    Capture("02-caves-root-front"),
    OrbitCamera(yaw_turns: 0.33333334, pitch_fraction: -0.15),
    Settle(60),
    Capture("03-caves-root-side"),
    OrbitCamera(yaw_turns: 0.33333334, pitch_fraction: 0.15),
    Settle(60),
    Capture("04-caves-root-rear"),
    Key("Backspace"),
    AwaitScreen("Title"),
]
