// Real Character movement from the central region to its published crossing.
// Distant region anchors remain manifested for follow-up integration evidence.
[
    AwaitScreen("Title"),
    StartScenario(name: "Seven Regions", seed: Some(703700113)),
    AwaitScreen("Gameplay"),
    AwaitTerrain,
    Key("H"),
    Key("C"),
    Settle(60),
    Capture("01-seven-regions-character-start"),
    ClickAnchor(name: "center_conflict_center", expected: (q: 0, r: 0, level: 16)),
    AwaitPartyIdle(max_frames: 2400),
    Settle(60),
    Capture("02-seven-regions-center-front"),
    OrbitCamera(yaw_turns: 0.33333334),
    Settle(45),
    Capture("03-seven-regions-center-side"),
    OrbitCamera(yaw_turns: 0.33333334),
    Settle(45),
    Capture("04-seven-regions-center-rear"),
    Key("Backspace"),
    AwaitScreen("Title"),
]
