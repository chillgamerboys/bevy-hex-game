// Focused tactical first-person proof on the step-heavy Mountains route.
[
    AwaitScreen("Title"),
    StartScenario(name: "Mountains", seed: Some(129704046)),
    AwaitScreen("Gameplay"),
    AwaitTerrain,
    Key("H"),
    AssertCameraMode(Map),
    Key("C"),
    AssertCameraMode(Character),
    Key("C"),
    AssertCameraMode(FirstPerson),
    Settle(45),
    Capture("01-first-person-mountains-start"),
    ClickAnchor(name: "low_bypass", expected: (q: -12, r: 6, level: 19)),
    AwaitPartyIdle(max_frames: 2400),
    AssertSelectedAt(expected: (q: -12, r: 6, level: 19)),
    Settle(60),
    Capture("02-first-person-low-bypass-front"),
    // Look away from the mountain wall toward the exposed traversal shelf.
    OrbitCamera(yaw_turns: 0.16666667, pitch_fraction: -0.1),
    Settle(45),
    Capture("03-first-person-low-bypass-look"),
    Key("C"),
    AssertCameraMode(Map),
    Settle(45),
    Capture("04-first-person-restored-map"),
    Key("Backspace"),
    AwaitScreen("Title"),
]
