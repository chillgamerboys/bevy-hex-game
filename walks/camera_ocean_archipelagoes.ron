// Seed-pinned review of the complete ocean, home landing, causeway, and wooded heart.
[
    AwaitScreen("Title"),
    StartScenario(name: "Ocean Archipelagoes", seed: Some(1592598566)),
    AwaitScreen("Gameplay"),
    AwaitTerrain,
    Key("H"),
    AssertCameraMode(Map),
    Settle(60),
    Capture("01-ocean-archipelagoes-complete-map"),

    Key("C"),
    AssertCameraMode(Character),
    Settle(45),
    Capture("02-ocean-archipelagoes-home-channel-character"),
    Key("C"),
    AssertCameraMode(FirstPerson),
    Settle(45),
    Capture("03-ocean-archipelagoes-home-channel-first-person"),

    Key("C"),
    AssertCameraMode(Map),
    ClickAnchor(name: "archipelago.home_beach", expected: (q: -17, r: 4, level: 11)),
    Settle(5),
    AwaitPartyIdle(max_frames: 3000),
    AssertSelectedAt(expected: (q: -17, r: 4, level: 11)),
    Key("C"),
    AssertCameraMode(Character),
    OrbitCamera(yaw_turns: 0.16666667),
    Settle(45),
    Capture("04-ocean-archipelagoes-causeway-character"),

    Key("C"),
    AssertCameraMode(FirstPerson),
    Key("C"),
    AssertCameraMode(Map),
    ClickTile(q: -20, r: 7, level: Some(11)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -20, r: 7, level: 11)),
    ClickTile(q: -23, r: 10, level: Some(9)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -23, r: 10, level: 9)),
    ClickTile(q: -26, r: 10, level: Some(10)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -26, r: 10, level: 10)),
    ClickTile(q: -26, r: 14, level: Some(9)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -26, r: 14, level: 9)),
    ClickTile(q: -27, r: 16, level: Some(9)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -27, r: 16, level: 9)),
    ClickTile(q: -24, r: 16, level: Some(11)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -24, r: 16, level: 11)),
    ClickTile(q: -21, r: 14, level: Some(11)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -21, r: 14, level: 11)),
    ClickTile(q: -19, r: 12, level: Some(10)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -19, r: 12, level: 10)),
    ClickTile(q: -17, r: 12, level: Some(11)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -17, r: 12, level: 11)),
    ClickTile(q: -13, r: 12, level: Some(14)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -13, r: 12, level: 14)),
    ClickTile(q: -9, r: 12, level: Some(16)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -9, r: 12, level: 16)),
    ClickTile(q: -5, r: 12, level: Some(16)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -5, r: 12, level: 16)),
    ClickTile(q: -1, r: 12, level: Some(16)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: -1, r: 12, level: 16)),
    ClickTile(q: 3, r: 12, level: Some(16)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: 3, r: 12, level: 16)),
    ClickTile(q: 7, r: 12, level: Some(16)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: 7, r: 12, level: 16)),
    ClickTile(q: 11, r: 12, level: Some(14)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: 11, r: 12, level: 14)),
    ClickTile(q: 13, r: 12, level: Some(12)),
    Settle(5),
    AwaitPartyIdle(max_frames: 600),
    AssertSelectedAt(expected: (q: 13, r: 12, level: 12)),
    Key("C"),
    AssertCameraMode(Character),
    Settle(45),
    Capture("05-ocean-archipelagoes-wooded-heart-character"),
    OrbitCamera(yaw_turns: -0.33333334),
    Settle(45),
    Capture("06-ocean-archipelagoes-satellite-coast-character"),
    Key("C"),
    AssertCameraMode(FirstPerson),
    Key("C"),
    AssertCameraMode(Map),
    // Reset both the camera state and the review origin before the close view.
    // The terminal tile is tree-adjacent; this higher protected ridge keeps the
    // first-person proof clear of trunks and canopy near planes.
    ClickAnchor(name: "archipelago.home_ridge", expected: (q: 10, r: 2, level: 16)),
    Settle(5),
    AwaitPartyIdle(max_frames: 4800),
    AssertSelectedAt(expected: (q: 10, r: 2, level: 16)),
    Key("C"),
    AssertCameraMode(Character),
    Key("C"),
    AssertCameraMode(FirstPerson),
    OrbitCamera(yaw_turns: 0.33333334, pitch_fraction: -0.05),
    Settle(45),
    Capture("07-ocean-archipelagoes-wooded-heart-first-person"),

    Key("Backspace"),
    AwaitScreen("Title"),
]
