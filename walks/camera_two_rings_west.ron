// Ordinary-network Character traversal through the south-west outlet and dry regions.
[
    AwaitScreen("Title"),
    StartScenario(name: "Two Rings", seed: Some(1592598566)),
    AwaitScreen("Gameplay"),
    AwaitTerrain,
    Key("H"),
    Key("C"),
    Settle(60),
    ClickAnchor(name: "fort_outlet_overlook", expected: (q: -26, r: 46, level: 13)),
    AwaitPartyIdle(max_frames: 4800),
    AssertSelectedAt(expected: (q: -26, r: 46, level: 13)),
    Settle(45),
    Capture("01-two-rings-outlet-waterfall-front"),
    OrbitCamera(yaw_turns: 0.33333334),
    Settle(45),
    Capture("02-two-rings-outlet-waterfall-side"),
    ClickAnchor(name: "fort_fort_courtyard", expected: (q: -45, r: 44, level: 17)),
    AwaitPartyIdle(max_frames: 3600),
    AssertSelectedAt(expected: (q: -45, r: 44, level: 17)),
    Settle(45),
    Capture("03-two-rings-fort-front"),
    OrbitCamera(yaw_turns: -0.33333334),
    Settle(45),
    Capture("04-two-rings-fort-side"),
    ClickAnchor(name: "caves_conflict_center", expected: (q: -44, r: 22, level: 9)),
    AwaitPartyIdle(max_frames: 3600),
    AssertSelectedAt(expected: (q: -44, r: 22, level: 9)),
    Settle(45),
    Capture("05-two-rings-caves-front"),
    OrbitCamera(yaw_turns: 0.33333334),
    Settle(45),
    Capture("06-two-rings-caves-side"),
    ClickAnchor(name: "volcano_bridge", expected: (q: -44, r: 0, level: 18)),
    AwaitPartyIdle(max_frames: 4800),
    AssertSelectedAt(expected: (q: -44, r: 0, level: 18)),
    Settle(45),
    Capture("07-two-rings-volcano-front"),
    OrbitCamera(yaw_turns: -0.33333334),
    Settle(45),
    Capture("08-two-rings-volcano-side"),
    Key("Backspace"),
    AwaitScreen("Title"),
]
