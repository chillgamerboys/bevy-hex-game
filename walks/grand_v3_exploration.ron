// Grounded exploration review: native-feel signoff remains a separate playtest.
[
    AwaitScreen("Title"),
    StartScenario(name: "Grand V3 Baseline", seed: Some(1592598566)),
    AwaitGameplay(max_seconds: 180),
    AwaitTerrain,
    Key("H"),
    AssertCameraMode(Map),
    Settle(30),
    Capture("00-grand-v3-map-overview"),
    Key("Backspace"),
    AwaitScreen("Title"),
    AwaitButton("Explore Grand V3 Map"),
    Click(name: "Explore Grand V3 Map"),
    AwaitGameplay(max_seconds: 180),
    AwaitTerrain,
    Settle(30),
    AssertExplorer(mode: "fly", grounded: false),
    Capture("01-fly-start"),
    Key("F"),
    Settle(60),
    AssertExplorer(mode: "walk", grounded: true),
    Capture("02-walk-start"),
    HoldKeys(keys: ["W"], frames: 35),
    HoldKeys(keys: ["Shift", "D"], frames: 25),
    Settle(45),
    AssertExplorer(mode: "walk", grounded: true, minimum_displacement: Some(1.0)),
    Capture("03-walk-and-run"),
    Key("Space"),
    Settle(8),
    AssertExplorer(mode: "walk", grounded: false),
    Capture("04-jump"),
    Settle(60),
    AssertExplorer(mode: "walk", grounded: true),
    OrbitCamera(yaw_turns: 0.25),
    Capture("05-walk-side"),
    Key("Space"),
    Settle(8),
    Key("F"),
    AssertExplorer(mode: "fly", grounded: false),
    Capture("06-fly-from-jump"),
    Key("F"),
    Settle(60),
    AssertExplorer(mode: "walk", grounded: true),
    Capture("07-landed"),
    Key("Backspace"),
    AwaitScreen("Title"),
    Click(name: "Explore Grand V3 Map"),
    AwaitGameplay(max_seconds: 180),
    AwaitTerrain,
    Settle(30),
    AssertExplorer(mode: "fly", grounded: false),
    Key("F"),
    Settle(60),
    AssertExplorer(mode: "walk", grounded: true),
    Key("Backspace"),
    AwaitScreen("Title"),
]
